{
  "name": "minihack-cohort1",
  "version": "1.0.0",
  "description": "Mini Hack Cohort 1 — Payments on Avalanche starter template",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:fuji": "hardhat run scripts/deploy.js --network fuji",
    "verify": "hardhat verify --network fuji",
    "node": "hardhat node",
    "clean": "hardhat clean",
    "client:dev": "cd frontend && npm install && npm run dev",
    "client:build": "cd frontend && npm run build",
    "client:preview": "cd frontend && npm run preview"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "axios": "^1.6.0",
    "cors": "^2.8.6",
    "dotenv": "^16.4.0",
    "ethers": "^6.11.0",
    "express": "^4.18.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
